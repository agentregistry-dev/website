---
title: arctl skill build
weight: 10
---

Note: `arctl skill build` is not a separate command. Use `arctl skill publish` to build and publish skills as Docker images.

See [arctl skill publish](/docs/reference/cli/arctl-skill-publish) for building and publishing skills.
